// test cases:
// - invalid commands
// - invalid rover start position

import Rover from '../../src/rover';
import statusEnums from '../../src/status-enums';

test('moo cow', () => {
  expect(typeof 'moo').toBe('string');
});
